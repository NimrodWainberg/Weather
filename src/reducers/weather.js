const weatherRducer = (
  state = { weathers: [], favorites: [], currentWeather: null },
  action
) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default weatherRducer;
